// <auto-generated>
//   Generated by Data Table Exporter from Buff.xlsx. DO NOT EDIT.
//   Regenerate via tools/data_table_exporter.
// </auto-generated>
using System;
using System.Collections.Generic;
using System.IO;
using System.Text;
using System.Threading;
using Google.Protobuf;
using Google.Protobuf.Collections;

namespace MmorpgClient.Table
{
    /// <summary>
    /// Auto-generated config manager for Buff.
    /// </summary>
    /// <remarks>
    /// 热更契约：Load() 整批建好新快照再原子换掉旧的。返回的行对象属于**当前**快照 —— 调用方
    /// 只存 id，不要长期持有引用；热更后旧引用不会崩，但会永远读到热更前的值。
    /// 每个访问方法都在开头取一次本地快照再用：同一次调用里若并发发生 Load，反复读字段会
    /// 拿到两个不同世代的快照（例如表缩小时按旧 Count 取下标就越界）。
    /// </remarks>
    public sealed class BuffTableManager
    {
        // 两个常量的大小写不一样，这不是笔误：json_gen 写的是 "buff.json"（原样表名），
        // binary_gen 写的是 "buff.pb"（全小写）。Windows 上大小写不敏感看不出来，
        // Android / iOS / Linux 上拼错一个字母就是 FileNotFound。
        public const string JsonFileName = "buff.json";
        public const string BinaryFileName = "buff.pb";

        private static readonly BuffTableManager s_instance = new BuffTableManager();

        private static readonly JsonParser s_jsonParser =
            new JsonParser(JsonParser.Settings.Default.WithIgnoreUnknownFields(true));

        private static readonly BuffTable[] s_emptyRows = new BuffTable[0];

        [ThreadStatic]
        private static Random s_random;

        // snapshot 是不可变快照的引用；volatile 保证 Load 里填好的字典对读线程可见
        // （volatile 写 = release 屏障），读侧无需加锁。
        private volatile Snapshot _snapshot;

        private BuffTableManager()
        {
            // 注意：不要把空快照做成 static readonly 字段 —— 它与 s_instance 同属静态
            // 初始化，声明顺序一变就会在构造期读到 null。这里用方法现造，与顺序无关。
            _snapshot = new Snapshot(new BuffTableData(), 0);
        }

        public static BuffTableManager Instance
        {
            get { return s_instance; }
        }

        /// <summary>Data file name for the given format.</summary>
        public static string FileName(bool useBinary)
        {
            return useBinary ? BinaryFileName : JsonFileName;
        }

        /// <summary>Immutable snapshot: parsed container plus every index built over it.</summary>
        private sealed class Snapshot
        {
            public readonly BuffTableData Container;
            public readonly RepeatedField<BuffTable> Rows;
            public readonly Dictionary<uint, BuffTable> KvData;
            public readonly Dictionary<double, IReadOnlyList<BuffTable>> IdxIntervalEffect;
            public readonly Dictionary<uint, IReadOnlyList<BuffTable>> IdxSubBuff;
            public readonly Dictionary<uint, IReadOnlyList<BuffTable>> IdxTargetSubBuff;

            public Snapshot(BuffTableData container, int capacity)
            {
                Container = container;
                Rows = container.Data;
                KvData = new Dictionary<uint, BuffTable>(capacity);
                IdxIntervalEffect = new Dictionary<double, IReadOnlyList<BuffTable>>();
                IdxSubBuff = new Dictionary<uint, IReadOnlyList<BuffTable>>();
                IdxTargetSubBuff = new Dictionary<uint, IReadOnlyList<BuffTable>>();
            }
        }

        // ---- Load ----

        /// <summary>Read "buff.pb" / "buff.json" from <paramref name="configDir"/> and swap in a new snapshot.</summary>
        public void Load(string configDir, bool useBinary)
        {
            string path = Path.Combine(configDir, FileName(useBinary));
            if (useBinary)
            {
                LoadFromBytes(File.ReadAllBytes(path));
            }
            else
            {
                LoadFromJson(File.ReadAllText(path, Encoding.UTF8));
            }
        }

        /// <summary>Backward-compatible overload — defaults to JSON.</summary>
        public void Load(string configDir)
        {
            Load(configDir, false);
        }

        /// <summary>Parse proto binary bytes. Use this on platforms where File IO is unavailable (WebGL, Android StreamingAssets).</summary>
        public void LoadFromBytes(byte[] raw)
        {
            Rebuild(BuffTableData.Parser.ParseFrom(raw));
        }

        /// <summary>Parse protobuf-JSON text. Unknown fields are ignored, matching the Java loader.</summary>
        public void LoadFromJson(string json)
        {
            Rebuild(s_jsonParser.Parse<BuffTableData>(json));
        }

        private void Rebuild(BuffTableData container)
        {
            RepeatedField<BuffTable> rows = container.Data;
            Snapshot snap = new Snapshot(container, rows.Count);

            for (int i = 0; i < rows.Count; i++)
            {
                BuffTable row = rows[i];
                snap.KvData[row.Id] = row;
                RepeatedField<double> intervalEffectElems = row.IntervalEffect;
                for (int j = 0; j < intervalEffectElems.Count; j++)
                {
                    Index(snap.IdxIntervalEffect, intervalEffectElems[j], row);
                }
                RepeatedField<uint> subBuffElems = row.SubBuff;
                for (int j = 0; j < subBuffElems.Count; j++)
                {
                    Index(snap.IdxSubBuff, subBuffElems[j], row);
                }
                RepeatedField<uint> targetSubBuffElems = row.TargetSubBuff;
                for (int j = 0; j < targetSubBuffElems.Count; j++)
                {
                    Index(snap.IdxTargetSubBuff, targetSubBuffElems[j], row);
                }
            }

            // 索引建完了,把所有桶从可变 List 冻成数组 —— 冻之前它们是可以被强转回 List 改掉的。
            Freeze(snap.IdxIntervalEffect);
            Freeze(snap.IdxSubBuff);
            Freeze(snap.IdxTargetSubBuff);

            // 原子换快照：到这一行为止读线程看到的都是上一代，之后看到的都是完整的新一代。
            _snapshot = snap;
        }

        // ---- FindAll ----

        /// <summary>All rows in table order. 属于当前快照，调用方只存 id，不要长期持有。</summary>
        public IReadOnlyList<BuffTable> FindAll()
        {
            Snapshot snap = _snapshot;
            return snap.Rows;
        }

        /// <summary>The raw parsed container (Java findAll() 的等价物)。</summary>
        public BuffTableData Container
        {
            get { return _snapshot.Container; }
        }

        /// <summary>按主键取一行，没有则返回 null。</summary>
        /// <remarks>热更契约：返回的对象属于当前快照，调用方**只存 id**，不要长期持有引用。</remarks>
        public BuffTable FindById(uint id)
        {
            Snapshot snap = _snapshot;
            BuffTable row;
            return snap.KvData.TryGetValue(id, out row) ? row : null;
        }

        /// <summary>按主键取一行，命中返回 true。</summary>
        public bool TryFindById(uint id, out BuffTable row)
        {
            Snapshot snap = _snapshot;
            return snap.KvData.TryGetValue(id, out row);
        }

        /// <summary>主键 -> 行。</summary>
        public IReadOnlyDictionary<uint, BuffTable> KvData
        {
            get { return _snapshot.KvData; }
        }

        /// <summary>Rows whose repeated field interval_effect contains <paramref name="key"/>.</summary>
        public IReadOnlyList<BuffTable> FindByIntervalEffectIndex(double key)
        {
            Snapshot snap = _snapshot;
            return Bucket(snap.IdxIntervalEffect, key);
        }

        /// <summary>Rows whose repeated field sub_buff contains <paramref name="key"/>.</summary>
        public IReadOnlyList<BuffTable> FindBySubBuffIndex(uint key)
        {
            Snapshot snap = _snapshot;
            return Bucket(snap.IdxSubBuff, key);
        }

        /// <summary>Rows whose repeated field target_sub_buff contains <paramref name="key"/>.</summary>
        public IReadOnlyList<BuffTable> FindByTargetSubBuffIndex(uint key)
        {
            Snapshot snap = _snapshot;
            return Bucket(snap.IdxTargetSubBuff, key);
        }

        // ---- Exists ----

        public bool Exists(uint id)
        {
            Snapshot snap = _snapshot;
            return snap.KvData.ContainsKey(id);
        }

        // ---- Count ----

        /// <summary>总行数。</summary>
        public int Count()
        {
            Snapshot snap = _snapshot;
            return snap.Rows.Count;
        }

        public int CountByIntervalEffectIndex(double key)
        {
            Snapshot snap = _snapshot;
            return Bucket(snap.IdxIntervalEffect, key).Count;
        }

        public int CountBySubBuffIndex(uint key)
        {
            Snapshot snap = _snapshot;
            return Bucket(snap.IdxSubBuff, key).Count;
        }

        public int CountByTargetSubBuffIndex(uint key)
        {
            Snapshot snap = _snapshot;
            return Bucket(snap.IdxTargetSubBuff, key).Count;
        }

        // ---- FindByIds (IN) ----

        /// <summary>Look up many ids at once.</summary>
        public List<BuffTable> FindByIds(IReadOnlyList<uint> ids)
        {
            Snapshot snap = _snapshot;
            List<BuffTable> result = new List<BuffTable>(ids.Count);
            for (int i = 0; i < ids.Count; i++)
            {
                BuffTable row;
                if (snap.KvData.TryGetValue(ids[i], out row))
                {
                    result.Add(row);
                }
            }
            return result;
        }

        // ---- RandOne ----

        /// <summary>Uniformly random row, or null when the table is empty.</summary>
        public BuffTable RandOne()
        {
            Snapshot snap = _snapshot;
            int count = snap.Rows.Count;
            if (count == 0)
            {
                return null;
            }
            return snap.Rows[NextInt(count)];
        }

        // ---- Where / First ----

        public List<BuffTable> Where(Predicate<BuffTable> pred)
        {
            Snapshot snap = _snapshot;
            List<BuffTable> result = new List<BuffTable>();
            RepeatedField<BuffTable> rows = snap.Rows;
            for (int i = 0; i < rows.Count; i++)
            {
                if (pred(rows[i]))
                {
                    result.Add(rows[i]);
                }
            }
            return result;
        }

        public BuffTable First(Predicate<BuffTable> pred)
        {
            Snapshot snap = _snapshot;
            RepeatedField<BuffTable> rows = snap.Rows;
            for (int i = 0; i < rows.Count; i++)
            {
                if (pred(rows[i]))
                {
                    return rows[i];
                }
            }
            return null;
        }

        // ---- internals ----

        private static void Index<TKey>(Dictionary<TKey, IReadOnlyList<BuffTable>> map, TKey key, BuffTable row)
        {
            // 建索引期间桶是 List(要 Add);Rebuild 末尾 Freeze() 会把它换成数组。
            // 这个向下转换只在 Freeze 之前成立,而 Index 只在 Rebuild 的循环里调用。
            IReadOnlyList<BuffTable> existing;
            List<BuffTable> bucket;
            if (map.TryGetValue(key, out existing))
            {
                bucket = (List<BuffTable>)existing;
            }
            else
            {
                bucket = new List<BuffTable>();
                map[key] = bucket;
            }
            bucket.Add(row);
        }

        private static IReadOnlyList<BuffTable> Bucket<TKey>(Dictionary<TKey, IReadOnlyList<BuffTable>> map, TKey key)
        {
            IReadOnlyList<BuffTable> bucket;
            return map.TryGetValue(key, out bucket) ? bucket : s_emptyRows;
        }

        /// <summary>
        /// 把索引里的桶从可变 List 冻成数组。**必须在 Rebuild 的最后调用**。
        ///
        /// 不冻的话,调用方拿到 IReadOnlyList 后强转回 List 就能改到所有读者共享的那一份 ——
        /// 这会直接推翻本类型宣称的「不可变快照」。冻成数组之后强转也改不动。
        /// </summary>
        private static void Freeze<TKey>(Dictionary<TKey, IReadOnlyList<BuffTable>> map)
        {
            List<TKey> keys = new List<TKey>(map.Keys);
            for (int i = 0; i < keys.Count; i++)
            {
                List<BuffTable> bucket = (List<BuffTable>)map[keys[i]];
                map[keys[i]] = bucket.ToArray();
            }
        }

        private static int NextInt(int exclusiveMax)
        {
            Random rng = s_random;
            if (rng == null)
            {
                rng = new Random(unchecked((Environment.TickCount * 397) ^ Thread.CurrentThread.ManagedThreadId));
                s_random = rng;
            }
            return rng.Next(exclusiveMax);
        }
    }
}
