{#- go_bit_index.go.j2 — ID→位序映射。产物落在 bit_index/<包名>/ 子目录,见 bit_index_gen._gen_go:
    本模板声明 `package <sheet>`,多张 bit_index 表若平铺在同一目录,Go 会因「一个目录两个包」
    整模块编译失败。空白控制(`{%-`)与 name_width 对齐是为了产出 gofmt 等价排版:
    gofmt 会把连续常量行的 `=` 对齐,ID_1 与 ID_10 不等长时不补空格就会被 gofmt -l 挑出来
    (旧模板靠「每条之间插一个空行」让每条自成对齐组来规避,现改为显式补齐)。 -#}
// Code generated by Data Table Exporter. DO NOT EDIT.

package {{ sheet | lower }}

const (
{%- for id, index in id_to_index.items() %}
	{{ ("ID_%s" % id) | ljust(name_width) }} = {{ index }}
{%- endfor %}
)

const MaxBitIndex = {{ max_bit_index }}
